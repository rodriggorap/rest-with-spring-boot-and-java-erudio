server:
  port: 8888
cors:
  originPatters: http://localhost:3000,http://localhost:8080,https://rodrigo.com.br
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    # properties:
      # hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
spring-doc:
springdoc:
  paths-to-match: /api/**/v1*/**
           
      
    
     
